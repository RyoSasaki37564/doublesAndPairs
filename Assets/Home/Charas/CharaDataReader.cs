using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharaDataReader : MonoBehaviour
{
    /// <summary>
    /// ユーザーの所持キャラ
    /// </summary>
    [SerializeField] List<GameObject> m_usersCharas = new List<GameObject>();



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
